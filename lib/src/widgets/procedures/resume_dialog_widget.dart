import 'package:flutter/material.dart';

class ResumeDialogWidget extends StatelessWidget {
  const ResumeDialogWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}